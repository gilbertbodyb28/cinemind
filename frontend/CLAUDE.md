# Vision UI lock (this folder)

Do not edit `src/index.css`, `src/App.css`, `tailwind.config.js`, or
`public/index.html` theme values. Copy existing glass / chip / pill / gold
rail.

See `/AGENTS.md`.
